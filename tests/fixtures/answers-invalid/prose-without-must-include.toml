# BREAKS: a 'prose' task needs a non-empty must_include list. An empty list
# would grade every submission as correct, which is worse than no grader.
schema   = 1
scenario = "99"
title    = "fixture"
time     = "x"
needs    = "x"
ticket   = "x"

[[tasks]]
id     = "1"
prompt = "p"
grader = "prose"
