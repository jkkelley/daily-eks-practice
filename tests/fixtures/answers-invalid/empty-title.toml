# BREAKS: every top-level string (scenario, title, time, needs, ticket) must be
# non-empty. Whitespace does not count as content.
schema   = 1
scenario = "99"
title    = "   "
time     = "x"
needs    = "x"
ticket   = "x"

[[tasks]]
id           = "1"
prompt       = "p"
grader       = "prose"
must_include = ["a"]
